# 00 Tutorial How-To

## 核心理念
- 先把运行环境和调用方式统一好，再学后续提示词技巧。
- 重点不是 prompt 本身，而是让所有 notebook 都能稳定跑通。

## 教程内容
- 安装依赖并准备 API key。
- 设置 `API_KEY` 与 `MODEL_NAME` 供后续章节复用。
- 定义统一的 `get_completion(prompt)` 调用函数。
- notebook 按顺序执行，避免变量缺失。

## 练习内容
- 无。
