`size()`返回

只读迭代器：`const_iterator`
```cpp
void printDeque(const deque<int>& d)
{
	for (deque<int>::const_iterator it = d.begin(); it != d.end(); it++)
		cout << *it;
}
```

`rbegin()` `rend()`方向迭代器
用`string::reverse_iterator rit = s.rbegin()`创建

重载`()`的**类**，其对象称为**函数对象**，也叫**仿函数**

用成员函数重载`<`或`()`等运算符以改变标准库函数或容器的排序规则，最好用`const`修饰参数和函数体

**`find()`的结尾：**
对于`<algorithm>`的算法`find()`和容器的成员函数`.find()`，如果没有找到目标，会返回该容器的结尾`.end()`

