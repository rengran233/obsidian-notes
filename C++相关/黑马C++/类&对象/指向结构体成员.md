如果结构标识符是结构名，使用句点运算符"."
```
class Person
{
public:
	int age;
};
int main()
{
	Person ob;
	ob.age = 18;
}
```

如果结构标识符是指向结构的指针，使用箭头标识符"->"
```
class Person
{
public:
	int age;
};
int main()
{
	Person *p = new Person; //创建一个无结构名的动态结构
	p -> age = 18; //"p->age"即被指向结构的age成员
	//p前没有*，指向成员而非结构
}
```
