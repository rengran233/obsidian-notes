```cpp
bool a[10] { 1 }; //第一个元素为1，其余为0
memset(a, 1, sizeof(a));
//(地址，填充，内存大小)，全部初始化为1，填充属于0~255
//memset()会将元素每个字节设置为填充，只能用于布尔和字符类型(1字节)
```

```cpp
#include <algotithm>
int a[10];
fill(a, a + 10, 1);
//(begin(), end(), ele)可以用于各种数据类型和容器
```
对于嵌套容器，`fill()`需要**逐行填充**