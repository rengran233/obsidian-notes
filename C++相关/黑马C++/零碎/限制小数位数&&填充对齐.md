`#include<iomanip>`
`cout << setw(n) << "awa"`
若`n`大于后面跟随的字符长度, 在字符前填充空格, 否则不起作用
`cout << setfill('*') << setw(n) << "awa"`
可以使用`setfill()`改变填充的字符, 填充数据类型应是`char`或`wchar_t`

```
int main()
{
	std::cout << std::fixed << std::setprecision(3) << 3.0 / 2;

	return 0;
}
```

