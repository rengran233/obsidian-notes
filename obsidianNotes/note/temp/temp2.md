**下载** **nvm** **管理** **node** **版本：直接安装** **node** **会安装在系统级文件夹下，而通过** **nvm** **不会。**

问题：使用 nvm 无法安装旧版本的 node 

包管理器：npm、yarn、pnpm

nvm、SSH、邮箱、lerna@6

npm版本过高

全局命令`-g`需要`sudo`权限

禁止vscode的内置ts工作，使用vue自带的

变量名和属性名相同时，JS对象可以省去属性名和：直接写变量名创建对象

你好👋，关于promotion-tool-bar这个组件有两点需要确认一下：
1. 分隔符使用的颜色#D9D9D9并不属于主题色，是否存在错误？
2. 根据设计稿，分隔符与前后两项的间距是24，而没分隔符的两项之间的间距是16；现在没有分隔符的两项之间间距是24，要按照设计稿来吗