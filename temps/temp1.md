`vitest`原生基于`vite`架构，可直接在`vite.config.js`配置

如果进程进入阻塞状态却不能被唤醒，将永远在阻塞队列占用资源，可能造成死锁

共享存储 消息传递 管道通信（管道是一个文件）

*几个原语的执行内容*

进程与程序

线程包含CPU现场？

临界资源 互斥
进入 临界 退出 剩余
单标志 双标志 - 先 后

自旋锁：适合用于多核CPU，在单核会死循环

实现互斥的P操作要在同步的P操作之后，会导致阻塞
缓冲区大小大于1，必须设置mutex信号量实现互斥

互斥锁：加锁失败切换线程
自旋锁：加锁失败忙等待
如果加锁的操作执行时间短则使用自旋锁，否则线程切换开销要比代码执行还大
读写锁：读锁可被多线程使用，写锁只能单独写入
读优先、写优先、读写公平
乐观锁：先允许操作，再检查冲突，冲突则回滚操作

