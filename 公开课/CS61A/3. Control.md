(1) environment: 
Call functions will create a frame, global and function frames constitute a environment

(2) `False` in python is refers to `None`, `''`, `0` instead of only `0`

(3) multiple return values
```python
def divide_exact(n, d):
	return n // d, n % d
quotient, reminder = divide_exact(2025, 10)
```

(4) statement, header, suite:
```python
while i < 3: } header   } statement
	i += 1      } suite }
	total += i  }       }
```

(5) return a string would be attached "" but print wouldn't


