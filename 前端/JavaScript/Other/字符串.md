三种字符串操作：`+`, `+=`, `concat()`

模板字符串：用\`\`包裹字符串，用`${}`解析变量及表达式，保留换行符

使用`string.indexOf(substr, startIndex)`寻找子串

使用`prompt(message, defaultValue);`读取用户输入并以字符串形式返回，如果用户不输入，返回`null`

`string.includes(substr, [opt]startIndex)`可以检查字符串是否包含字串并返回布尔值
`string.slice(startIndex, [opt]endIndex)`返回子串，如果索引值为负数将从末尾倒数

`charCodeAt(index)`
`fromCharCode(num)
`indexOf()`
`includes()`
`slice()`
`toUpperCase()`/`toLowerCase()`
`replace()`/`replaceAll()`
`repeat()`
`trim()`/`trimStart()`/`trimEnd()`
`prompt()`