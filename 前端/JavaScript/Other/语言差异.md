箭头函数：比`lambda`更强大

JS的面向对象特性基于原型

在不使用分号分割语句时，不同于python通过排版和缩进分割语句，JS使用自动补全，但是可能出问题，所以推荐在JS中使用`;`分割语句

JS是动态语言，不像C++/Java可以通过编译期检查函数重载来匹配不同的参数列表。如果使用参数列表传参需要使用`undefined`占位

TS可以使用`type`和`|`联合字面量类型，相当于其他语言的枚举