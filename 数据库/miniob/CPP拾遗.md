CPP中构造函数处理成员变量的顺序应与在类中定义相同

`const char*`浅拷贝，`string`深拷贝

在类声明中不需要加类名限定符

CMake: 读取CMakeLists.txt，检查环境和依赖库创建构建计划
何时删除构建文件：更改CMake，出现未知链接错误，切换编译器或编译类型
make: 执行构建计划，编译和链接文件，仅重新编译被更改的文件

在系统设计中，“应该使用指针吗？”这类问题的答案通常不是简单的“是”或“否”，而是基于对**所有权 (Ownership)、生命周期 (Lifetime) 和性能 (Performance)** 的权衡。

如果用到多态，基类的析构函数必须**是虚函数**，否则如果有声明为基类的派生类对象析构，将不会动态调用派生类的析构函数，导致内存泄漏

引用`TYPE &`与常量指针`const TYPE*`的区别：引用不能为空

`new/delete`, `malloc/free`的使用要配套