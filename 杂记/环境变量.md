```
1.set "VCPKG_ROOT=D:\Dev\vcpkg\" 
2.set PATH=%VCPKG_ROOT%;%PATH%
```
以如上代码为例：
1.设置环境变量VCPKG_ROOT
2.将VCPKG_ROOT的路径加入PATH最前端（优先检索）
```
%VCPKG_ROOT%;%PATH% ↓ 变量展开后 D:\Dev\vcpkg\;C:\Windows\System32;C:\Program Files\...
```
`%`的作用为**动态解析并插入其他变量的值**，改变`PATH`值后`%PATH%`仍然会给出对应值