- 修正了`Tank`类的错误，将初始化中的基础速度改为`1.0f`，否则无法移动
- 修正了`Bullet`类的错误，将初始化中的标量速度（导致无法移动）改为了矢量
- 为`Bullet`类新增了边界销毁
- 新增了`Core/GameTypes.h`，用于收录所有公用的结构体/枚举类，因为在初始化子弹时用到了`Tank`类中定义的方向枚举类，改了很多命名空间
- 修正了子弹贴图不能显示的bug，原因可能是`pixmapName`不对或者build内容过时
- 使用方向枚举类+`QPointF`类型需要大量使用`switch`转换为向量，不简洁，修改为使用`QVector2D`来存储八个方向
- 7.1/上午  改变`MovableObject`移动逻辑，新增`isMoving`标志判断是否移动，让坦克保持速度向量不为`0`，为子弹指示方向
- 7.1/下午  花费超长时间，将方向向量和速率分开了，方向向量和速率没有区分开导致乘了两次速率，子弹速度极大，瞬间超出场景销毁。关于子弹速度问题，原因是**Bullet类同时也是MovableObject类，你在两个类中都对方向向量乘了一次速率！！！**
- 7.1/ 傍晚  功能分离思想真的很重要
- 7.1/21:43  增加UI自适应屏幕分辨率的功能，可以最大化最小化和自由拖动调整窗口大小。但是观察到在画面在重绘时出现白线
- 7.1/22:02  白线系QGraphicsView在重绘背景与贴图时时序不当，默认视口更新模式为MinimalViewportUpdate，只重绘能包住变化区域的最小矩形（脏区域），受计算误差和抗锯齿影响可能不够精密。改用FullViewportUpdate强制每次重绘整个画面解决问题（有兼顾性能和效果的折中方案，但不管用）
- 7.1/22:03  注意到每次启动游戏不能正确显示贴图，必须要调整一下窗口，推测为Qt事件处理与初始布局相关的时序问题
- 7.1/22:17  在GameEngine创建完所有对象后发射信号强制刷新画面，解决问题
- 7.1/23:03  注意到GameState中只储存了游戏状态枚举类，把它挪到GameTypes
- 更新了UI，现在有主菜单了。并且在开始游戏时自动最大化窗口
- 新增了镜头跟随功能，在镜头跟随模式会不断以坦克为中心绘制画面
- 通过在MainWindow中存储一个m_sceneBound，让画面只在镜头模式变化时更新一次（与跟随模式每次位置更新都变化不同），修正了镜头固定模式下会显示整张地图的bug。这样GameEngine在坦克位置变化时只需要发送镜头大小，MainWindow也不需要重复计算。同时GameEngine实时更新镜头大小，也给了子弹动态边界销毁的尺度，一举两得。
- LevelManager在GameObject是个空指针，解引用空指针导致闪退，已修改
- 梳理了GameObjects中所有游戏对象类的构造函数，现在可以通过修改Config中的参数直接改变各类的基础参数
- 尝试了两种方法实现对鼠标移动的追踪。因为GameGraphicsView会最先拦截鼠标移动事件，首先尝试用过滤器用GameWidget代替GameGraphicsView处理，但是不知道为什么失败了。之后直接实例化对GameGraphicsView操作，成功实现。而鼠标左键事件可以直接用GamaWidget捕捉
- 暂时没有炮管贴图，绘制一个矩形出来代替
- 实现了炮管的360°旋转，由鼠标操控
- 初步掌握地图文件结构和解析方法，拟通过动态加载实现部分加载超大地图，防止实体过多过于卡顿
- 拟使用QPainter绘制地面以节省性能，其他需要交互的对象使用QGraphicsItem
- 失败，准备修改架构，不让MainWindow得知LevelSystem的存在，而是由GameEngine中转来自LevelSystem的信号
- 原来的一种墙类的gid只有一种，修改Wall的派生类的构造函数，使其能接受gid
- **及时保存版本很重要**
- 尝试一下午，放弃了解析多图层的地图文件，直接使用单图层
- 坦克对象有两个贴图，需要特别处理分别销毁
- 完成了动画特效。给特效和子弹设置方向，子弹用速度向量计算角度
- 关卡资源加载失败也会导致后续的图片等资源加载失败，抓最早的报错
- 增加贴图切换，通过在需要切换的类中维护一个QString实现。如果该QString为空，则在渲染的函数中设置默认值
- 不行！静态成员必须进行类外定义，不能空着。类内只能赋值字面量类型
- 改变了非墙对象下墙体的创建方式，现在子弹不会创建一堆墙体了